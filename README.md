# 3rd-party-ts-boilerplate

This repository contains 3rd party REST API boilerplate Express.js, Typescript, TypeORM based.

## Logs

A logs directory must be present on root of dist directory.

`$ mkdir ./dist/logs`